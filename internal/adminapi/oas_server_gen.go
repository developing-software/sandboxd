// Code generated by ogen, DO NOT EDIT.

package adminapi

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// ApproveHost implements approve-host operation.
	//
	// The code is what the worker printed when it enrolled. A worker started with the operator's join
	// token skips this and is approved on hello. 409 when the host is not pending; 400 when the code does
	// not match.
	//
	// POST /hosts/{id}/approve
	ApproveHost(ctx context.Context, req *ApproveBody, params ApproveHostParams) error
	// ListHosts implements list-hosts operation.
	//
	// Every enrolled host, with what it reported about itself.
	//
	// GET /hosts
	ListHosts(ctx context.Context) ([]HostView, error)
	// RevokeHost implements revoke-host operation.
	//
	// The host is rejected on its next hello, and on the socket it is holding now. The row stays — a
	// revoked host is remembered, not removed.
	//
	// POST /hosts/{id}/revoke
	RevokeHost(ctx context.Context, params RevokeHostParams) error
	// NewError creates *ErrorStatusCode from error returned by handler.
	//
	// Used for common default response.
	NewError(ctx context.Context, err error) *ErrorStatusCode
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h   Handler
	sec SecurityHandler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, sec SecurityHandler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		sec:        sec,
		baseServer: s,
	}, nil
}
